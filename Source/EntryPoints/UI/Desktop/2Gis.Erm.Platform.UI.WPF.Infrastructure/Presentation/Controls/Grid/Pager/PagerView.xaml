<UserControl x:Class="DoubleGis.Erm.Platform.UI.WPF.Infrastructure.Presentation.Controls.Grid.Pager.PagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:blendability="clr-namespace:DoubleGis.Erm.Platform.UI.WPF.Infrastructure.Presentation.Controls.Grid.Blendability"
             mc:Ignorable="d"
             d:DataContext="{x:Static blendability:DesignTimeProvider.Pager}">
    <StackPanel Orientation="Horizontal">
        <Button Content="⇤" Command="{Binding FirstCommand}" />
        <Button Content="←" Command="{Binding PrevCommand}" />
        <TextBlock Text="{Binding BeforePageText.Title}" VerticalAlignment="Center"/>
        <TextBlock Text="{Binding CurrentPageNumber, Mode=OneWay}" VerticalAlignment="Center" Margin="5 0 0 0"/>
        <Label Content="{Binding TotalPagesCount, Mode=OneWay}" ContentStringFormat="{Binding AfterPageText.Title}" VerticalAlignment="Center" Margin="0 0 0 0"/>
        <Button Content="→"  Command="{Binding NextCommand}" />
        <Button Content="⇥" Command="{Binding LastCommand}" />
    </StackPanel>
</UserControl>
